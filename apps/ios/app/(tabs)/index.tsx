import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function HomeScreen() {
  return (
    <PlaceholderScreen
      emoji="🏠"
      title="首頁"
      subtitle="P3 會接上動態牆與限時動態"
    />
  );
}
