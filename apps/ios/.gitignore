# Expo / React Native (apps/ios)
.expo/
dist/
web-build/

# Native projects generated by `expo prebuild` (managed workflow: regenerate, don't commit)
/ios
/android

# Metro
.metro-health-check*

# Debug
npm-debug.*
*.orig.*

# macOS
.DS_Store

# Note: GoogleService-Info.plist IS committed here intentionally — it carries no
# secret (client config only) and EAS Build needs it. Do not add it to ignore.
